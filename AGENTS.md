# AGENTS.md

# 项目目标

本项目是一个 A 股量化分析与市场状态识别系统。

核心模块包括：

* 股票长期产业分类
* 板块生命周期识别
* 板块强度评分
* 市场主线排名
* 龙头股票筛选
* 多股票 PK
* 多日龙头确认
* 市场环境分析

本项目中：

**算法正确性和状态机正确性优先于开发速度。**

程序能够运行，不代表算法正确。

以下情况都不能被视为功能正确：

* 编译成功
* 没有异常
* demo 可以运行
* 输出看起来合理
* 某一个历史案例结果正确

# 核心工程原则

任何代码修改都必须证明：

1. 找到了真正的错误原因。
2. 修改针对的是根因，而不是表面现象。
3. 修改没有破坏其他日期、股票或板块。
4. 修改后的行为与系统原始设计一致。

禁止为了让某个 demo 或某个历史日期通过而增加特殊判断。

# 修改代码之前

任何 bug 修复或算法修改开始前，必须：

1. 阅读相关模块完整实现。
2. 找到真实执行入口。
3. 追踪完整调用链。
4. 确认上游输入数据。
5. 确认中间计算结果。
6. 确认下游消费者。
7. 尽可能复现用户报告的问题。
8. 在修改代码之前确定根因。

禁止仅根据：

* 函数名
* 文件名
* 单个代码片段
* 最终输出

猜测错误原因。

算法问题必须追踪真实数据。

# 完整数据链路检查

涉及市场分析时，根据问题检查完整链路：

原始行情数据

→ 股票特征

→ 股票评分

→ 板块聚合

→ 板块生命周期

→ 板块强度

→ 市场排名

→ 龙头候选

→ 龙头 PK

→ 多日确认

→ demo / API / UI 输出

如果最终结果错误，不允许直接修改展示层。

必须首先确认错误究竟发生在哪一个阶段。

# 修改原则

优先进行：

**最小且正确的修改。**

禁止：

* 重写与当前问题无关的模块
* 修改无关公式
* 随意改变阈值
* 为了让 demo 通过而改变数据定义
* 针对某个历史日期写特殊逻辑
* 针对某只股票代码写特殊逻辑
* 针对某个板块名称写特殊逻辑
* hardcode 预期结果
* 删除暴露问题的异常样本
* 增加用于隐藏错误的 fallback
* 吞掉异常
* 静默修改原有评分含义
* 在没有证据时调整算法参数

除非任务明确要求改变算法定义，否则必须尽可能保持原有行为。

# 板块生命周期规则

板块生命周期必须被视为：

**时间序列状态机。**

可能存在的状态包括但不限于：

* 未启动
* 启动
* 发酵
* 加速
* 高潮
* 分歧
* 退潮
* 退潮确认
* 退潮后修复
* 弱反弹
* 强修复
* 二次启动
* 第二波

如果状态定义依赖历史数据，则禁止只根据单日数据判断。

修改生命周期算法时必须检查：

* 前一状态
* 当前状态
* 当前观察值
* 状态持续天数
* 状态转换条件
* 允许的状态转移
* 禁止的状态转移
* 是否出现重复转移
* 是否错误重置状态
* 退潮后的企稳过程
* 修复与二次启动的区别
* 第一波与第二波的区别

特别检查：

**退潮后不能长期停留在“退潮后反弹”。**

如果板块已经完成：

退潮

→ 企稳

→ 强度重新积累

→ 核心股重新走强

→ 持续确认

则必须检查系统是否应该进入：

二次启动 / 第二波

而不是持续标记为：

退潮后反弹。

# 时间序列规则

所有涉及历史数据的算法必须重点检查：

* T 日数据
* T-1 数据
* T-2 数据
* 滚动窗口
* 状态持续时间
* 日期对齐
* 交易日对齐
* 缺失交易日
* 新增股票数据
* 停牌数据
* 未来数据泄漏

严禁：

在计算 T 日结果时使用 T+1 或之后的信息。

任何可能产生 future data leakage 的代码都属于严重错误。

# 市场排名规则

修改市场排名时必须检查：

* 原始评分
* 每日评分
* 多日评分
* 平均评分
* 加权评分
* 时间衰减
* normalization
* 权重
* 板块生命周期权重
* 缺失数据
* 候选板块范围
* 排名方向
* tie 处理
* eligibility
* filter 顺序
* 最终排序

禁止看到排名错误就直接修改 sort。

必须检查：

错误是否发生在排序之前。

# 龙头股票筛选规则

龙头判断必须区分：

* 单日强度
* 多日强度
* 持续性
* 相对板块强度
* 市场地位
* 涨幅
* 成交额
* 流动性
* 市值
* 换手
* 回撤
* 抗跌性
* 启动领先程度
* 核心辨识度
* 多日稳定性

如果当前系统采用多股票 PK：

不得仅根据某一天最高分直接确定龙头。

需要检查：

候选池

→ Top N

→ 单日 PK

→ 多日 PK

→ 龙头持续性

→ 最终确认

如果增加市值限制，也必须作为明确规则实现，禁止隐式影响最终排名。

# Bug 修复标准流程

任何 bug 必须遵循：

复现问题

→ 追踪数据

→ 定位错误阶段

→ 建立根因假设

→ 使用证据验证

→ 确定 Root Cause

→ 建立 Regression Test

→ 最小修改

→ Build

→ Test

→ 运行真实 Demo

→ 检查实际输出

→ 检查附近日期

→ 检查其他股票 / 板块

→ Review Git Diff

如果其中任何一步失败：

**继续调试。**

不得直接宣布任务完成。

# 回归测试规则

确认一个 bug 后，在条件允许时必须建立 regression test。

Regression Test 应满足：

修复前：

FAIL

修复后：

PASS

测试应该验证：

算法行为

而不是：

代码实现细节。

禁止为了让测试通过而修改 expected result。

如果 expected result 确实需要修改，必须说明原来的预期为什么错误。

# Golden Cases

对于：

* 板块生命周期
* 龙头判断
* 市场排名
* 第二波识别
* 退潮识别

优先建立历史 Golden Cases。

Golden Case 必须来自：

真实历史数据 + 人工确认结果。

禁止 Codex 自己猜测历史正确答案。

Golden Cases 应覆盖：

* 真启动
* 假启动
* 加速
* 高潮
* 分歧
* 真退潮
* 假退潮
* 退潮确认
* 弱反弹
* 强修复
* 二次启动
* 第二波
* 假突破
* 龙头切换
* 龙头持续
* 龙头掉队

算法修改后必须尽量运行这些历史案例。

# 时间序列 Bug 验证

禁止仅验证问题发生当天。

如果 T 日出现问题，至少检查：

T-2

T-1

T

T+1

T+2

如果涉及：

生命周期

第二波

趋势

多日评分

龙头切换

则应根据实际情况检查更长窗口，例如：

T-10 ～ T+5。

# Demo 规则

Demo 只是生产算法的调用者。

禁止为 Demo 建立一套独立业务逻辑。

Demo 应尽可能调用与正式系统完全相同的计算路径。

如果 Demo 输出错误，需要依次检查：

1. 输入数据
2. 数据清洗
3. 特征计算
4. 股票评分
5. 板块聚合
6. 生命周期
7. 排名
8. 龙头筛选
9. 序列化
10. 展示逻辑

禁止一发现 Demo 错误就直接修改 Demo。

# Demo 错误特别规则

如果程序：

* 能运行
* 没报错
* 能正常生成 JSON
* 页面也能正常显示

但结果逻辑错误，

这仍然属于严重 Bug。

必须按照算法错误处理，而不是 UI 问题处理。

# 强制验证流程

任何代码修改完成后必须尽可能执行：

1. Build
2. 全量现有测试
3. 当前模块测试
4. Regression Test
5. Golden Cases
6. 真实 Demo
7. 实际结果检查
8. 临近日期检查
9. 其他股票 / 板块检查
10. Git Diff Review

只有在这些验证完成之后才能判断任务是否完成。

# 对抗性检查

修复完成后，不要只证明：

“现在可以工作。”

还必须主动尝试证明：

“这个修改可能是错的。”

主动测试：

* 阈值边界
* 空数据
* 缺失值
* 重复数据
* 极端值
* 平分
* Top N 边界
* 状态反复切换
* 连续上涨
* 连续下跌
* 急跌后反弹
* 弱修复
* 强修复
* 二次启动
* 第二波
* 龙头切换
* 单日异常脉冲

# Git Diff 检查

任务结束前必须检查 git diff。

重点寻找：

* 无关修改
* hardcode
* 临时 debug 代码
* console 输出
* 魔法数字
* 重复逻辑
* silent fallback
* 被吞掉的异常
* 未解释的默认值
* 日期特殊判断
* 股票代码特殊判断
* 板块名称特殊判断
* future data leakage
* off-by-one
* 日期错位
* rolling window 错误

# 完成条件

以下条件满足后才能宣布任务完成：

* 已复现原始问题
* 已找到明确 Root Cause
* 有代码或运行数据作为证据
* 已实施最小修改
* 已运行 Build
* 已运行相关测试
* Regression Test 通过
* Golden Cases 未出现异常回归
* 已运行真实 Demo
* 已人工检查实际输出
* 已检查时间窗口附近数据
* 已完成 Git Diff Review

如果其中关键验证没有执行：

禁止写：

“已完全修复”

“问题已经解决”

“实现正确”

应该明确说明哪些部分尚未验证。

# 最终报告格式

每次 Bug 修复完成后，使用以下结构报告：

## ROOT CAUSE

真正导致错误的原因。

## 修改内容

修改了哪些文件。

修改了什么算法。

为什么这样修改。

## 验证过程

实际运行了哪些：

* build
* tests
* regression test
* golden cases
* demo

## 实际结果

修复后的真实输出和行为。

## 回归风险

此次修改可能影响哪些：

* 日期
* 股票
* 板块
* 状态
* 排名
* 龙头判断

## 未验证内容

如果存在没有验证的内容，必须明确列出。

不得隐藏未验证事项。

# Code Review

进行代码审查时，同时遵守项目根目录中的：

`code_review.md`
