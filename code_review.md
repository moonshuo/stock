# Code Review Rules

代码 Review 时：

**正确性优先于代码风格。**

Review 的核心不是：

“代码写得好不好看。”

而是：

“这个修改会不会让算法悄悄算错。”

# P0 — 严重错误

优先检查：

* Future Data Leakage
* 使用未来交易日数据
* 日期错位
* 股票与日期 merge 错位
* 状态机非法转换
* 龙头排名方向错误
* 数据泄漏
* hardcode 历史结果
* 针对股票代码特殊处理
* 针对日期特殊处理
* 针对板块特殊处理
* 删除验证逻辑
* 吞掉严重异常

发现这类问题优先报告。

# P1 — 算法正确性

检查：

* 公式是否正确
* 分数方向是否正确
* 权重是否正确
* normalization 是否正确
* threshold 是否合理地实现了当前定义
* 时间窗口是否正确
* rolling 是否正确
* state duration 是否正确
* 缺失数据如何处理
* 重复数据如何处理
* 排序是否正确
* filter 顺序是否正确

# P2 — 生命周期正确性

特别检查：

* 启动条件
* 启动确认
* 加速
* 高潮
* 分歧
* 退潮
* 退潮确认
* 退潮修复
* 二次启动
* 第二波

检查状态是否：

过早进入

过晚进入

无法退出

重复进入

错误 reset

永久停留

重点检查：

退潮之后重新走强时，系统是否仍错误长期停留在：

“退潮后反弹”。

# P3 — 时间序列正确性

重点寻找：

* off-by-one
* T 与 T-1 混淆
* 交易日与自然日混淆
* window 起点错误
* window 终点错误
* shift 方向错误
* stale state
* incorrect reset
* future leakage
* 最新日期被漏掉
* 第一天处理错误
* 最后一天处理错误

# P4 — 市场排名正确性

检查：

* 候选板块 universe
* eligibility
* filter
* raw score
* daily score
* multi-day score
* average score
* weighted score
* decay
* normalization
* sorting
* tie
* Top N

特别确认：

最终排名错误是否实际上来自：

上游评分。

不要默认认为排序函数有问题。

# P5 — 龙头判断正确性

检查：

* 龙头候选池
* Top N 初筛
* 单日强度
* 多日强度
* 领先时间
* 板块相对强度
* 市场辨识度
* 涨幅
* 成交额
* 换手
* 市值
* 流动性
* 回撤
* 抗跌
* 持续性
* 多股票 PK
* 多日确认

检查是否出现：

某股票只因为一天得分最高就永久成为龙头。

# P6 — 回归风险

判断修改是否影响：

* 其他行业
* 其他板块
* 其他股票
* 更早日期
* 更晚日期
* 启动
* 退潮
* 修复
* 第二波
* 龙头
* 市场排名

特别关注：

修复一个案例之后，是否破坏另一个历史案例。

# P7 — 可疑修复

以下代码需要重点标记：

* 某个日期专用 if
* 某个股票代码专用 if
* 某个板块专用 if
* 没有解释的 magic number
* 为 demo 增加的特殊逻辑
* 新增 fallback
* try/catch 后忽略异常
* 删除 assertion
* 删除 validation
* 修改 expected test
* 修改历史数据
* 静默改变默认参数

必须判断：

这是正确业务规则，

还是为了让当前案例通过的 workaround。

# P8 — Demo 一致性

检查 Demo 是否调用：

真实生产算法。

如果 Demo 自己实现：

* 生命周期
* 排名
* 龙头
* 打分

需要重点警告。

Demo 不应该维护独立业务算法。

# P9 — 测试质量

检查新增测试是否：

真正覆盖 Bug。

应该满足：

修复前失败

修复后成功。

检查是否只测试：

happy path。

如果是时间序列问题，应检查：

目标日期之前

目标日期

目标日期之后。

# P10 — 代码质量

正确性确认之后，再检查：

* 重复逻辑
* 复杂函数
* 可读性
* 命名
* 无用代码
* 临时日志
* debug print
* dead code
* 注释是否与代码一致

不要为了代码风格建议大范围重构业务算法。

# Review 方法

不要只阅读最终修改文件。

必要时追踪：

调用者

↓

当前函数

↓

下游消费者

并检查修改对完整数据流的影响。

# Review 输出格式

按照严重程度输出：

## CRITICAL

可能导致：

* 数据泄漏
* 未来函数
* 大面积错误结果
* 生命周期严重错误
* 排名严重错误

## HIGH

会导致明确业务结果错误。

## MEDIUM

在特定输入下可能导致错误。

## LOW

代码质量、维护性或较低风险问题。

每个问题需要提供：

文件：

代码位置：

问题：

触发条件：

为什么错误：

可能影响：

建议修改：

# Review 原则

不要为了找问题而找问题。

如果没有发现实质性问题，应明确说明：

没有发现明确的 correctness issue。

不要把：

个人代码风格偏好

包装成：

严重 Bug。

# 最终重点

本项目 Review 最重要的问题永远是：

**程序虽然正常运行，但结果是否可能是错误的？**
